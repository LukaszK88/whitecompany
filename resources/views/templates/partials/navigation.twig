<nav class="navbar navbar-inverse navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar3">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path_for('home') }}"><img src="{{ base_url() }}/img/swords.png" height="100px">
            </a>
            <a class="navbar-brand" href="{{ path_for('home') }}"><img src="{{ base_url() }}/img/kaka.png" height="100px">
            </a>

        </div>
        <div id="navbar3" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">

                {% if auth.check %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                            {% if auth.user.image %}
                            <img class="img-circle center-block nav-photo" src="{{ base_url() }}{{ auth.user.image }}" >
                                 {% else %}
                                     <img class="img-circle center-block nav-photo" src="{{ base_url() }}/img/profile_placeholder.png">

                            {% endif %}
                            {% if auth.admin %}
                                GOD
                            {% endif %}
                            <span class="caret"></span></a>
                            <ul class="dropdown-menu" role="menu">
                                <li><a href="{{ path_for('get.profile.page',{ userId : auth.user.id}) }}">Profile</a></li>
                                <li><a href="#" data-toggle="modal" data-target="#updateProfile">Update details</a></li>
                                <li><a href="{{ path_for('auth.password.change') }}">Change password</a></li>
                                <li class="divider"></li>
                                <li><a href="{{ path_for('auth.signout') }}">Log out</a></li>
                            </ul>
                {% else %}
                    <li><a href="#" data-toggle="modal" data-target="#signIn" class="login" >Log in</a></li>
                    <li><a href="#" data-toggle="modal" data-target="#signUp" class="login" >Register</a></li>

                {% endif %}
            </ul>

        </div>
        <!--/.nav-collapse -->
    </div>
    <!--/.container-fluid -->
</nav>